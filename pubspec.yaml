name: trigger_generator
description: Code generator for Trigger, the state management.
version: 0.1.0
repository: https://github.com/BLeAm/trigger_generator

environment:
  sdk: ^3.10.7

# Add regular dependencies here.
dependencies:
  analyzer: ^10.2.0
  build: ^4.0.4
  source_gen: ^4.2.0
  path: ^1.9.0
  trigger: ^0.1.0+1

dev_dependencies:
  build_runner: ^2.12.2
